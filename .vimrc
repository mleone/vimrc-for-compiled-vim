set nocompatible " fix key bindings

set backspace=2 " fix backspace and delete keys

syntax on " turn on code-highlighting

" set soft tabs with 2-space indent for ruby development
set noexpandtab
set tabstop=2
set shiftwidth=2
